/* Re-exported for backward compatibility — use @/lib/settings for new code. */
export {
  type AiSettings,
  type Settings,
  SETTINGS_KEY,
  DEFAULT_AI_SETTINGS,
  DEFAULT_SETTINGS,
  getSettings,
  saveSettings,
  getAiSettings,
  saveAiSettings,
} from './settings';
